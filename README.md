1. var is a function scoped that which can we redeclare and reassign. 
let is a block scoped, it can reassigned but not redeclared in same slope.
const is totally blocked scoped which er cannot reassign or redeclare


2. The spread operator (...) is used to expand elements of an array or object into another array or object. normally it's used for copying or merging arrays and objects.


3. map() creates a new array by applying a function to each element.
filter() creates a new array containing elements which is satisfy a condition.
forEach() is execute a function for each element .but it does not return any new array.


4. An arrow function is a shorter syntax to writing function in JavaScript.like const add = (a,b) => a+b;


5. Template literals are  way to create a string using backticks ` ` and allow embedding variables using ${}.